name: mlproject_env
channels:
  - defaults
dependencies:
  - python=3.12.7
  - pip
  - pip:
      - pandas
      - numpy
      - scikit-learn
      - seaborn
      - matplotlib
      - mlflow==2.19.0
